# ScoreCAM-Replication
ScoreCAM-Replication is a faithful implementation of the Score-CAM method for visualizing class-specific regions in convolutional neural networks without using gradients. It computes the importance of each activation map by measuring the model’s forward response to masked inputs and combines them to generate an interpretable heatmap.
